/*function formatName(user) {
  return user.firstName + ' ' + user.lastName;
}

function getTeamsList() {
    return $.getJSON('http://localhost:8000/teams')
    .then(function(data) {
        return data.results;
    });
}

var user = {
  firstName: 'Harper',
  lastName: 'Perez'
};

var element = (
  <h1>
    Hello, {formatName(user)}!
  </h1>
);

ReactDOM.render(
  element,
  document.getElementById('root')
);*/